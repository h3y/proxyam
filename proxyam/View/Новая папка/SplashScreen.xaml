<UserControl x:Class="proxyam.View.SplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:proxyam.View"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="150" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <Grid Background="Black" Opacity="0.4" />
        <materialDesign:Card Height="48" Width="48" materialDesign:ShadowAssist.ShadowDepth="Depth2"
                             VerticalAlignment="Stretch">
            <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" 
                         Height="28" Width="28" />
        </materialDesign:Card>
    </Grid>
</UserControl>